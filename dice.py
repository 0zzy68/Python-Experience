import random
prints = int(input('How many times do you want to roll a dice?: '))
diceNum = random.randint(1,6)
for num in range(prints):
	if prints != prints:
		print('invalid insertion')
		continue
	diceNum = random.randint(1,6)
	print(diceNum)